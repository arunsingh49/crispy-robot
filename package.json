{
	"name": "wellnezv6",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"admin": "npm run start --prefix admin",
		"api": "npm run start --prefix api",
		"website": "npm run start --prefix website",
		"dev": "concurrently \"npm run admin\" \"npm run api\" \"npm run website\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"concurrently": "^5.3.0"
	}
}
